# Copy to .env and fill in values. Do NOT commit your real secrets.
# e.g., cp .env.example .env

# Required by stream_ensemble_test.py
SUPABASE_POST_URL=https://your-endpoint.example.com/ingest

# Optional auth header bearer token (if your endpoint requires it)
SUPABASE_SERVICE_ROLE=your-service-role-secret
